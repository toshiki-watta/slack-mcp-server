{
  "name": "slack-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "supergateway --stdio \"npx -y @modelcontextprotocol/server-slack\" --port $PORT --cors --streamable-stdio"
  },
  "dependencies": {
    "@modelcontextprotocol/server-slack": "^0.6.2",
    "supergateway": "^3.4.3"
  }
}
